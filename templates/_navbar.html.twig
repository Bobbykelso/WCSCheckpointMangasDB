
<nav class="navbar navbar-expand-lg navbar-light bg-light">
{#    <img src="{{ asset('build/images/navbaranime.png') }}" alt="image mangas navbar">#}
    <div class="container-fluid">
        <a class="nav-link" href="/">Oeuvres</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="/category">Catégories</a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <ul class="navbar-nav">
                        <li class="user-connexion">
                            <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    </ul>
                {% else %}
                    <ul class="navbar-nav">
                        <li class="user-connexion">
                            <a href="/login" class="nav-link">Se connecter</a>
                        </li>
                        <li class="user-connexion">
                            <a href="/register" class="nav-link" >S'inscrire</a>
                        </li>
                    </ul>
                {% endif %}
            </ul>
            <form id="searchForm" class="nav-search" action="{{ path('home_search') }}" method="get">
                <div id="searchBox">
                    <input class="nav-input" type="text" name="b" id="searchField" autocomplete="off">
                    <ul class="autocomplete" id="autocomplete"></ul>
                </div>
                <button class="btn btn-light btn-outline-success">Rechercher</button>
            </form>
        </div>
    </div>
</nav>
